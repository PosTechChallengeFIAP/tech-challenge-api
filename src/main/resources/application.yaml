spring:
    application:
        name: tech-challenge
    datasource:
        url: jdbc:mysql://localhost:3306/tech_chalenge
        username: root
        password: mysql
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: none
        database-platform: org.hibernate.dialect.MySQLDialect
    liquibase:
        change-log: classpath:db/migration.xml